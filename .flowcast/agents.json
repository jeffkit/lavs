{
  "//": "lavs agent pool（项目级）。lavs 是 TS+Python 双端 SDK。minimax/deepseek 走 provider 网关（不依赖 claude 登录）；claude 需 claude auth login。pge --agent minimax 会自动回退 planner/evaluator 到 minimax（pge >=2026-08-04 的 profile 回退修复）。",
  "agents": {
    "minimax": {
      "executor": "claude",
      "provider": "anthropic-minimax",
      "extraArgs": ["--dangerously-skip-permissions"],
      "timeout": 600000
    },
    "deepseek": {
      "executor": "claude",
      "provider": "anthropic-deepseek",
      "extraArgs": ["--dangerously-skip-permissions"],
      "timeout": 600000
    },
    "claude": {
      "executor": "claude",
      "timeout": 600000,
      "extraArgs": ["--dangerously-skip-permissions"]
    },
    "codex": {
      "executor": "codex",
      "timeout": 600000
    },
    "claude-reviewer": {
      "_comment": "reviewer profile：协议/双端一致性专家视角，用于 evaluator 角色。",
      "executor": "claude",
      "provider": "anthropic-minimax",
      "timeout": 300000,
      "extraArgs": ["--dangerously-skip-permissions"],
      "extraPromptPrefix": "你是协议与 SDK 一致性专家，重点审查：TS/Python 双端语义是否一致、协议变更是否同步 SPEC.md 与 schema/、类型定义是否完整。"
    }
  }
}
